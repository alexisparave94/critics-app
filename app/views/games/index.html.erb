<p style="color: green"><%= notice %></p>

<h1>Games</h1>
<%= link_to "New game", new_game_path %>

<div id="games">
  <% @games.each do |game| %>
    <%= render "game_card", game: game %>
    <p>
      <%= link_to "Show", game %>
    </p>
  <% end %>
</div>

